# 🛍️ 동시성 제어를 고려한 주문/결제 시스템 (사전과제)

## 📖 프로젝트 개요
이 프로젝트는 **e-커머스 주문 시스템의 핵심 로직**을 구현한 과제입니다.
특히 **재고 차감 시 발생하는 동시성 문제**를 해결하고, **유연한 확장성**을 고려하여 설계했습니다.

## 🛠️ 기술 스택
- **Java 1.8**, **Spring Boot 2.x**
- **JPA (Hibernate)**, **H2 Database**
- **JUnit 5** (통합 테스트 검증)

## 💡 핵심 구현 내용 및 문제 해결

### 1. 동시성 제어 (Concurrency Control)
- **문제 상황:** 다수의 사용자가 동시에 주문을 요청할 경우, 재고 정합성이 깨지는 **갱신 분실(Lost Update)** 문제 발생 가능성 확인.
- **해결:** `Pessimistic Lock (비관적 락)`을 적용하여 데이터 무결성 보장.
- **검증:** `List<Thread>`를 활용한 테스트 코드로 **91명 동시 주문 시 재고가 정확히 차감됨**을 검증 완료.

### 2. 확장성을 고려한 설계 (Design Pattern)
- **전략 패턴 (Strategy Pattern):** - 벤더사(Amadon, Coumang)마다 다른 **암호화 방식**을 `VendorEncryptionStrategy` 인터페이스로 추상화.
    - 새로운 벤더사가 추가되어도 기존 코드를 수정하지 않는 **OCP(개방-폐쇄 원칙)** 준수.
- **이벤트 기반 아키텍처 (Event-Driven):**
    - 재고 부족 시 발생하는 입고 요청 로직을 `Spring Event`와 `@Async`로 분리.
    - 주문 트랜잭션과 입고 요청 로직을 분리하여 **주문 응답 속도 향상** 및 **관심사 분리**.

### 3. 테스트 (Testing)
- **통합 테스트:** `OrderServiceTest`를 통해 [주문 -> 재고차감 -> 이벤트발행 -> 입고요청] 전체 프로세스 검증.
- **가독성:** `@DisplayName`을 활용하여 테스트 시나리오 명세화.
